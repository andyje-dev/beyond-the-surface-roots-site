---
import { Image } from "astro:assets";
import { nav } from "../../config/site";
import ThemeToggle from "./ThemeToggle.tsx";
import logoDark from "../../../public/subterranean-cartography-dark.png";
import logoLight from "../../../public/subterranean-cartography-light.png";

const currentPath = Astro.url.pathname;
---

<header
  class="sticky top-0 z-50 border-b"
  style={`background-color: var(--bg-deep); border-color: var(--text-ghost);`}
>
  <nav
    class="flex items-center justify-between"
    style={`max-width: 1400px; margin: 0 auto; padding: 1rem 2.5rem;`}
    aria-label="Primary navigation"
  >
    <!-- Logo -->
    <a
      href="/"
      class="flex items-center gap-2 no-underline hover:no-underline"
      style={`color: var(--accent-primary); font-family: var(--font-mono); font-weight: 500; font-size: var(--text-xl);`}
    >
      <Image
        src={logoDark}
        alt=""
        aria-hidden="true"
        class="logo-dark"
        width={60}
        height={60}
        loading="eager"
        style="flex-shrink: 0; border-radius: 4px;"
      />
      <Image
        src={logoLight}
        alt=""
        aria-hidden="true"
        class="logo-light"
        width={60}
        height={60}
        loading="eager"
        style="flex-shrink: 0; border-radius: 4px;"
      />
      <span><span style={`color: var(--text-primary);`}>bt</span><span>sr</span></span>
    </a>

    <!-- Nav links -->
    <ul class="hidden md:flex items-center gap-6 list-none" role="list">
      {
        nav.map(({ label, href }) => {
          const isActive =
            currentPath === href || currentPath.startsWith(href + "/");
          return (
            <li>
              <a
                href={href}
                class="no-underline hover:no-underline transition-colors"
                style={`
                  font-family: var(--font-sans);
                  font-weight: 500;
                  font-size: var(--text-sm);
                  color: ${isActive ? "var(--accent-primary)" : "var(--text-secondary)"};
                `}
                aria-current={isActive ? "page" : undefined}
              >
                {label}
              </a>
            </li>
          );
        })
      }
    </ul>

    <div class="flex items-center gap-4">
      <!-- Theme toggle -->
      <ThemeToggle client:load />

      <!-- Mobile menu button -->
      <button
        class="md:hidden p-2"
        style={`color: var(--text-secondary); background: none; border: none; cursor: pointer;`}
        aria-label="Open menu"
        aria-expanded="false"
        aria-controls="mobile-menu"
        id="mobile-menu-btn"
      >
        <svg
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          aria-hidden="true"
        >
          <line class="hamburger-top" x1="3" y1="6" x2="21" y2="6"></line>
          <line class="hamburger-mid" x1="3" y1="12" x2="21" y2="12"></line>
          <line class="hamburger-bot" x1="3" y1="18" x2="21" y2="18"></line>
        </svg>
      </button>
    </div>
  </nav>

  <!-- Mobile nav panel -->
  <div
    id="mobile-menu"
    class="mobile-menu md:hidden"
    style={`border-color: var(--text-ghost);`}
  >
    <ul class="flex flex-col gap-4 list-none" role="list" style={`padding: 1.25rem 2.5rem 1.5rem;`}>
      {
        nav.map(({ label, href }) => {
          const isActive =
            currentPath === href || currentPath.startsWith(href + "/");
          return (
            <li>
              <a
                href={href}
                class="no-underline hover:no-underline"
                style={`
                  font-family: var(--font-sans);
                  font-weight: 500;
                  font-size: var(--text-base);
                  color: ${isActive ? "var(--accent-primary)" : "var(--text-secondary)"};
                `}
              >
                {label}
              </a>
            </li>
          );
        })
      }
    </ul>
  </div>
</header>

<style is:global>
  /* Theme-aware logo */
  [data-theme="dark"] .logo-dark { display: block; }
  [data-theme="dark"] .logo-light { display: none; }
  [data-theme="light"] .logo-dark { display: none; }
  [data-theme="light"] .logo-light { display: block; }
</style>

<style>
  .mobile-menu {
    overflow: hidden;
    max-height: 0;
    opacity: 0;
    border-top: 1px solid transparent;
    transition: max-height 0.25s ease, opacity 0.2s ease, border-color 0.2s ease;
  }

  .mobile-menu.open {
    max-height: 300px;
    opacity: 1;
    border-color: var(--text-ghost);
  }

  /* Hamburger → X animation */
  .hamburger-top,
  .hamburger-mid,
  .hamburger-bot {
    transition: transform 0.25s ease, opacity 0.15s ease;
    transform-origin: center;
  }

  [aria-expanded="true"] .hamburger-top {
    transform: translate(0, 6px) rotate(45deg);
  }

  [aria-expanded="true"] .hamburger-mid {
    opacity: 0;
  }

  [aria-expanded="true"] .hamburger-bot {
    transform: translate(0, -6px) rotate(-45deg);
  }

  @media (prefers-reduced-motion: reduce) {
    .mobile-menu {
      transition: none;
    }
    .hamburger-top,
    .hamburger-mid,
    .hamburger-bot {
      transition: none;
    }
  }
</style>

<script>
  function initMobileMenu() {
    const btn = document.getElementById("mobile-menu-btn");
    const menu = document.getElementById("mobile-menu");

    btn?.addEventListener("click", () => {
      const isOpen = menu?.classList.toggle("open");
      btn.setAttribute("aria-expanded", String(isOpen));
      btn.setAttribute("aria-label", isOpen ? "Close menu" : "Open menu");
    });
  }
  initMobileMenu();
  document.addEventListener("astro:after-swap", initMobileMenu);
</script>
